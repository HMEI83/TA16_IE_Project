import React from "react";

import "./Card.css";

export const Card1 = (props) => {
  return <div className="card1">{props.children}</div>;
};

export const Card2 = (props) => {
  return <div className="card2">{props.children}</div>;
};
